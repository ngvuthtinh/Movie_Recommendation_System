export interface RoomInfo {
    roomID: string;
    roomName: string;
    roomPassword: string;
}

export const roomInfoDefault: RoomInfo = {
    roomID: "12",
    roomName: "TESTFE1",
    roomPassword: "1",
}